# QEMU (aarch64) linker
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

# DK2 (armv7) linker — set via env CARGO_TARGET_ARMV7_UNKNOWN_LINUX_GNUEABIHF_LINKER in build.sh
# [target.armv7-unknown-linux-gnueabihf]
# linker is set by stm32-builder's /tmp/arm-ca-gcc wrapper
