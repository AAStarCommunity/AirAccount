# AirAccount CA Node.js 环境配置

# 服务器配置
PORT=3002
NODE_ENV=development
LOG_LEVEL=info

# CORS配置
CORS_ORIGIN=http://localhost:3000

# WebAuthn配置
RP_NAME=AirAccount
RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:3002

# 数据库配置 (SQLite)
DATABASE_PATH=./data/airaccount.db

# TEE配置
TEE_CA_CLIENT_PATH=../../packages/airaccount-ca/target/debug/airaccount-ca

# 安全配置
SESSION_TTL_SECONDS=3600
CHALLENGE_TTL_SECONDS=300

# 架构原则提醒
# 重要：用户的Passkey凭证存储在客户端设备中
# 此节点只提供临时服务，用户需要保留恢复信息